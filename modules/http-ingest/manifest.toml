name     = "http-ingest"
version  = "1.0"
kind     = "source"
provides = [
  "http.ingest.received",
  "note.*",
  "shortcut.*",
]
max_body_bytes = 10485760

[[http.routes]]
method = "POST"
path   = "/ingest/{source}"

[[http.routes]]
method = "PUT"
path   = "/blobs"

name     = "http-ingest"
version  = "1.0"
kind     = "source"
provides = [
  "http.ingest.received",
  "note.*",
  "shortcut.*",
]
listen   = ":8080"
max_body_bytes = 10485760  # 10 MiB; large attachments should use blob_ref (see docs)
